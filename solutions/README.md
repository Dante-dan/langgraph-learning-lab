# 解题检查点

这里不提供可直接复制的完整答案，而提供评审标准：

- 节点是否只返回它负责的局部更新？
- 分支函数是否无副作用？
- 并行写入同一字段时是否定义 reducer？
- `Command` 是否真的同时需要 update 与 goto？
- interrupt 是否配套稳定的 thread_id 与 checkpointer？
- 恢复或重试时，写操作是否会重复执行？
- State 是否保持 JSON 安全，运行时依赖是否放在 Context？
- 循环是否有明确退出条件？

通过这些检查点后，再把示例中的 `topic` 替换成自己的业务问题。
